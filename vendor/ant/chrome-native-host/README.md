# @ant/chrome-native-host

Local implementation of the Claude in Chrome native-messaging bridge protocol. It mirrors the pure TypeScript native host flow from the local Claude Code source tree and exposes the runtime entrypoint plus protocol helpers.
