# @ant/claude-ssh

Local release-manifest adapter derived from the official desktop bundle's embedded `claude-ssh-releases` metadata. It exposes the same pinned version, checksums, platform list, and deterministic download URL construction used by the bundled SSH manager.
